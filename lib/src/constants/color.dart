import 'package:flutter/material.dart';

class ColorConstants {
  static const primaryHex = 0xFF002855;
  static const secondaryHex = 0xFFffd000;
  static const greyHex = 0xFFefefef;
  
  static const primary = Color(primaryHex);
  static const secondary = Color(secondaryHex);
  static const grey = Color(greyHex);
}